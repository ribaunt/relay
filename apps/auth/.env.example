# Relay Auth (RP)

# Override the IdP URL for local dev
RELAY_SERVICE_ID_ORIGIN=http://localhost:3001

# Session secret (dev default used if omitted)
RELAY_SESSION_SECRET=dev-relay-auth-session-secret-not-for-production

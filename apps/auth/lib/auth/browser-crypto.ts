/**
 * @deprecated Import directly from @relay/core instead.
 * The Relay class handles key management. Use relay.unlock() / relay.lock().
 */
export { decryptMasterKeyWithPassword } from "@relay/crypto"
